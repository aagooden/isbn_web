	<h2>Please enter possilbe ISBN numbers below.</h2>
	<h2><%= session[:message] %></h2>

	<%CSV.foreach("local_isbn.csv") do |row|%>
		<%if row == [] || row == [" "]%>
		<%elsif row[2] == session[:f_name] && row[3] == session[:l_name]%>
			<p><%=row[0] %>: <%=row[1]%></p>
		<% end %>
		<% end %>

	<form method="post" action="input">
		<input type="text" name="isbn" autofocus required>
		<input type="submit" value="Submit">
	</form>

<form action="index" method="post">
    <input type="submit" value="Click to view ISBN's" name="Submit">
</form>



<% session[:isbn].each do |num|  %>
 <h2><%= num[0] %>: <%= num[1] %></h2>
<% end %>
